// import 'dotenv/config';
// import validateEnv from './utils/validateEnv'
// import App from './main';

// validateEnv();

// const app = await new App(Number(process.env.PORT) || 3000);

// app.listen();

// export default app